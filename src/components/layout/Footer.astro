---
import { portfolioData } from '../../data/portfolio';
---

<footer class="py-12 px-4 border-t border-frappe-surface-0 bg-frappe-mantle">
  <div class="max-w-4xl mx-auto text-center">
    <p class="mb-4 text-frappe-subtext-1">
      ¿Interesado en trabajar juntos? ¡Hablemos!
    </p>
    <div class="flex justify-center gap-4">
      {portfolioData.socialLinks.map((link) => (
        <a 
          href={link.url} 
          target={link.name === "Email" ? undefined : "_blank"}
          rel={link.name === "Email" ? undefined : "noopener noreferrer"}
          class="gap-2 px-4 py-2 border border-frappe-surface-0 text-frappe-subtext-0 bg-transparent rounded-md hover:bg-frappe-surface-0 hover:text-frappe-text transition-all flex items-center"
        >
          {link.name === "GitHub" && (
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"></path>
            </svg>
          )}
          {link.name === "LinkedIn" && (
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 8a6 6 0 016 6v7h-4v-7a2 2 0 00-2-2 2 2 0 00-2 2v7h-4v-7a6 6 0 016-6zM2 9h4v12h-4V9zM4 6a2 2 0 100-4 2 2 0 000 4z"></path>
            </svg>
          )}
          {link.name === "Email" && (
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" data-astro-cid-anhloy43=""></path>
            </svg>
          )}
          {link.name}
        </a>
      ))}
    </div>
    <p class="text-sm mt-8 text-frappe-subtext-0">
      © 2024 Lukas Riveros. Todos los derechos reservados.
    </p>
  </div>
</footer>
